/* ARRAYS
 *
 * Um array armazena valores do mesmo tipo, em sequência. Apesar de o JavaScript permitir criar arrays com valores de tipos distintos, não se trata de uma boa prática.
 *
 * Sintaxe: var array = [value, value, value]
 *
 * métodos:
 *   array[position] - acessa o valor contido na posição especificada.
 *   length - retorna o número de valores existentes em um array.
 *   push - adiciona valores ao final de um array.
 *   unshift - adiciona valores no início de um array
 *
 *   concat
 *   every
 *   filter
 *   forEach
 *   join
 *   indexOf
 *   lastIndexOf
 *   map
 *   reverse
 *   slice
 *   some
 *   sort
 *   toString
 *   valueOf
 *
 */
